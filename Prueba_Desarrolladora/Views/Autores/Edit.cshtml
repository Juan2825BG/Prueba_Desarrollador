@model Prueba_Desarrollador.Models.Db.Autore

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-7">
            <div class="mb-5 text-center">
                <h2 class="fw-bold text-dark">Editar Autor</h2>
                <p class="text-muted">Actualiza el nombre del autor seleccionado</p>
            </div>

            <div class="table-rounded-wrapper p-5 shadow-sm bg-white">
                <form asp-action="Edit">
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-4"></div>

                    <input type="hidden" asp-for="AutorId" />

                    <div class="form-group mb-5">
                        <label asp-for="Nombre" class="form-label fw-bold text-dark mb-3">Nombre del Autor</label>
                        <div class="input-group custom-input-container">
                            <span class="input-group-text bg-transparent border-0 px-3">
                                <i class="bi bi-person-gear text-muted"></i>
                            </span>
                            <input asp-for="Nombre"
                                   class="form-control border-0 py-3 ps-2"
                                   placeholder="Ej: Gabriel García Márquez" />
                        </div>
                        <span asp-validation-for="Nombre" class="text-danger small mt-2 d-block"></span>
                    </div>

                    <div class="d-flex justify-content-between align-items-center mt-5 pt-2">
                        <a asp-action="Index" class="btn-return-simple text-decoration-none">
                            <i class="bi bi-arrow-left"></i> Cancelar
                        </a>

                        <button type="submit" class="btn-add-submit px-4 py-2">
                            <span>Guardar cambios</span>
                            <i class="bi bi-save-fill ms-2"></i>
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>


</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
