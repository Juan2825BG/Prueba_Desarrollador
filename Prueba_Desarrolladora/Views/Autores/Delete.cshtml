@model Prueba_Desarrollador.Models.Db.Autore

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-7">
            <div class="mb-5 text-center">
                <h2 class="fw-bold text-danger">Eliminar autor</h2>
                <p class="text-muted">Esta acción no se puede deshacer.</p>
            </div>

            <div class="table-rounded-wrapper p-5 shadow-sm bg-white border-top border-danger border-5">
                <div class="mb-4">
                    <h4 class="text-dark fw-bold">
                        <i class="bi bi-person-x-fill text-danger me-2"></i>
                        Confirmar Eliminación
                    </h4>
                    <hr />
                </div>

                <div class="row mb-5 py-3 bg-light rounded-3 mx-1">
                    <div class="col-sm-12 text-center">
                        <span class="fw-bold text-muted text-uppercase small d-block mb-2">
                            @Html.DisplayNameFor(model => model.Nombre)
                        </span>
                        <p class="fs-3 text-dark fw-bold mb-0">@Html.DisplayFor(model => model.Nombre)</p>
                    </div>
                </div>

                <form asp-action="Delete">
                    <input type="hidden" asp-for="AutorId" />

                    <div class="d-flex justify-content-between align-items-center">
                        <a asp-action="Index" class="btn-return-simple text-decoration-none">
                            <i class="bi bi-arrow-left"></i> Cancelar 
                        </a>

                        <button type="submit" class="btn-delete-glass px-4 py-2">
                            <span class="btn-text">Eliminar </span>
                            <div class="icon-container">
                                <i class="bi bi-trash3-fill"></i>
                            </div>
                        </button>
                    </div>
                </form>
            </div>

            <div class="mt-4 px-3 text-center">
                <small class="text-muted">
                    <i class="bi bi-info-circle me-1"></i>
                    Si este autor tiene libros asociados, asegúrate de haberlos gestionado antes de eliminar, en caso de no gestionarlos se eliminaran todos los libros asociados a este autor.
                </small>
            </div>
        </div>
    </div>

</div>